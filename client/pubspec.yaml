name: navigation_client
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.5.1+7

environment:
  sdk: ^3.12.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  collection: ^1.19.0
  http: ^1.2.0
  permission_handler: ^11.3.1
  latlong2: ^0.9.1
  geolocator: ^13.0.2
  flutter_svg: ^2.3.0
  maplibre_gl: ^0.26.0
  shared_preferences: ^2.3.2
  share_plus: ^10.1.4
  device_info_plus: ^11.2.0
  package_info_plus: ^8.1.2
  webview_flutter: ^4.14.0
  indoor_pdr_core:
    path: ../packages/indoor_pdr_core
  pointer_interceptor: ^0.10.1+2

  # 제품 UI의 semantic token과 공통 컴포넌트. 아래 SHA는 디자인시스템 PR #8의
  # 검증된 커밋 위에 정보 행 복사 글자 정렬 수정 하나를 얹은 것이다
  # (`fix/info-row-copy-baseline`). 브랜치가 아니라 SHA로 못박아 개발자·CI가 항상
  # 같은 화면을 빌드하게 한다. 병합 뒤에도 이 객체 SHA는 그대로 유효하다.
  #
  # **디자인시스템 main으로 올리지 않는다.** main은 PR #8 커밋과 갈라져 있어
  # (lib에서만 36파일·630줄 차이) 팔레트·테마·경로 플래너가 통째로 바뀐다. 올릴
  # 때는 그것이 이 앱에서 의도한 변경인지 따로 판단한다.
  routex_design_system:
    git:
      url: https://github.com/Routex-labs/routex-design-system.git
      ref: 302675e0240816ff778ce0c7b525fddb86e67847
      path: packages/routex_design_system
  url_launcher: ^6.3.2
  # 공유 링크 수신. cold start의 최초 URI와 실행 중 URI stream을 함께 준다.
  app_links: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

  # 타이머가 있는 코드를 위젯 없이 시험한다. 위치 스트림 수명
  # (gps_session_test.dart)이 재연결 백오프와 벙어리 감시를 초 단위로 확인하는데,
  # tester.pump는 위젯 트리를 요구하고 실제 시계로는 12초를 기다릴 수 없다.
  fake_async: ^1.3.1

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# objective_c 9.4.1 calls xcrun from its build hook even when Xcode already
# supplies SDKROOT. Xcode 26 can return empty output in that nested build
# environment, so use the repository-pinned SDK resolver until upstream ships
# an equivalent fix. See packages/objective_c_patched/PATCHES.md.
dependency_overrides:
  objective_c:
    path: ../packages/objective_c_patched

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  assets:
    # 매장 상세 이미지는 파일을 하나씩 적지 않고 디렉터리째 등록한다. 어떤 파일을
    # 쓸지는 서버 오버레이(`resources/store_details/*.json`(Routex-labs/fastapi))가 정하는데,
    # 등록을 빠뜨려도 빌드는 성공하고 실행 중에 카드만 빈 채로 뜬다 — 메뉴를 30종으로
    # 늘렸을 때 실제로 31개가 미등록으로 남아 있었다. 한쪽 목록을 지우는 편이 두 목록을
    # 맞춰 두는 것보다 낫다.
    - assets/place_details/
    - assets/mock/sample_building.json
    - assets/events/
    - assets/floors/
    - assets/mock/hyundai_floor_1f.json
    - assets/mock/hyundai_floor_map.svg
    - assets/mock/demo_floor_map_v5.svg
    - assets/icons/place_location_pin.svg
    - assets/branding/routee_marker.png

  # 앱 전체 기본 글꼴. 지정하지 않으면 플랫폼 기본(iOS Apple SD Gothic Neo,
  # Android Noto Sans CJK, 웹 브라우저 기본)이 쓰여 같은 화면이 기기마다 다르게
  # 보인다. Pretendard는 Apple SD Gothic Neo를 대체하려고 만들어진 글꼴이라
  # iOS 화면의 변화가 가장 작고, 자폭이 좁아 칩·업종 줄처럼 폭이 빠듯한 자리에
  # 여유가 생긴다. 라이선스는 OFL이며 assets/fonts/OFL.txt를 함께 배포한다.
  #
  # Flutter는 브라우저와 달리 글꼴을 동적으로 서브셋하지 않는다. 여기 올린 파일이
  # 웹에서 통째로 내려가므로(weight당 약 1.5MB) weight를 늘릴 때는 용량을 함께 본다.
  # 앱이 실제로 요청하는 400·500·600·700·800은 모두 같은 Pretendard v1.3.9
  # static face로 등록한다. 일부만 등록하면 엔진이 가장 가까운 굵기로 대체해
  # (예: w600→w700) 화면마다 의도한 위계가 달라진다.
  fonts:
    - family: Pretendard
      fonts:
        - asset: assets/fonts/Pretendard-Regular.otf
        - asset: assets/fonts/Pretendard-Medium.otf
          weight: 500
        - asset: assets/fonts/Pretendard-SemiBold.otf
          weight: 600
        - asset: assets/fonts/Pretendard-Bold.otf
          weight: 700
        - asset: assets/fonts/Pretendard-ExtraBold.otf
          weight: 800
    # 제목 세리프. **가변 폰트 파일 하나**로 400~900을 다 쓴다(축은 wght 하나).
    # 굵기는 pubspec이 아니라 쓰는 자리에서 FontVariation으로 고른다 — 여기에
    # weight를 여러 줄 적으면 같은 파일이 굵기마다 한 벌씩 등록된다.
    # 원본 사이트가 제목에 쓰는 것과 같은 폰트다(docs/client/thehyundai-event-source.md).
    - family: PlayfairDisplay
      fonts:
        - asset: assets/fonts/PlayfairDisplay-Variable.ttf

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
