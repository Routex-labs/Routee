<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- usesCleartextTraffic: 개발용 로컬 API 서버(http://<PC LAN IP>:8000)에
         실기기로 접속하려면 평문 HTTP 허용이 필요하다. Dart http는 자체 소켓이라
         이 정책과 무관하게 동작하지만, MapLibre 네이티브(벡터 타일 요청)는
         Android 네트워크 정책을 따르므로 이 설정이 없으면 실내 지도 타일만
         조용히 차단돼 지도가 비어 보인다. -->
    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- 장소 공유 링크. autoVerify가 켜져 있으면 OS가 설치 직후
                 https://<host>/.well-known/assetlinks.json 을 직접 받아 가서
                 이 앱이 그 주소의 것인지 확인한다. 확인되면 링크가 브라우저를
                 거치지 않고 곧바로 앱으로 온다.

                 host는 백엔드와 같은 주소다 — 그 서버가 증명 파일과 미설치
                 fallback 페이지를 함께 낸다(backend/app/routers/links.py). -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="navigation-api-465890645804.asia-northeast3.run.app"
                    android:pathPrefix="/place/"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- 매장 상세의 공식 채널 링크를 외부 브라우저로 열기 위해 필요하다.
             Android 11부터 다른 앱을 조회하려면 여기 선언해야 하고, 없으면
             url_launcher가 "열 수 있는 앱이 없다"고 판단해 조용히 실패한다. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
    </queries>
</manifest>
